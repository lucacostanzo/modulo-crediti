import { SubscriberStats } from "./types";
export declare function createEndpoint(): {
    onKeepAlive: (subscriberId: string, date: Date) => void;
    onStatsUpdated: (subscriberId: string, stat: SubscriberStats) => void;
    listen: () => void;
};
